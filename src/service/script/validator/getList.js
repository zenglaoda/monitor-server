const { level, status } = require('../../../scheme/script');
const { page, pageSize, projectId, sTime, eTime } = require('../../../scheme/share');

module.exports = {
  status, 
  level,
  projectId,
  page,
  pageSize,
  sTime,
  eTime,
};